[project]
name = "echoapi"
version = "0.1.0"
description = "Simple echo API with auth"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dishka>=1.6.0",
    "fastapi>=0.115.12",
]
